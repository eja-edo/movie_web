<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"
        xmlns:video="http://www.google.com/schemas/sitemap-video/1.1"
        xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9
                            http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd
                            http://www.google.com/schemas/sitemap-image/1.1
                            http://www.google.com/schemas/sitemap-image/1.1/sitemap-image.xsd
                            http://www.google.com/schemas/sitemap-video/1.1
                            http://www.google.com/schemas/sitemap-video/1.1/sitemap-video.xsd">

  <!-- Video sitemap entries -->
  {% for movie in movies %}
  <url>
    <loc>{{ domain }}/detail/{{ movie.movie_id }}</loc>
    <lastmod>{% if movie.release_date %}{{ movie.release_date|date:"Y-m-d" }}{% else %}2023-11-15{% endif %}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
    <video:video>
      <video:thumbnail_loc>{{ domain }}{{ movie.poster_url }}</video:thumbnail_loc>
      <video:title>{{ movie.title }}</video:title>
      <video:description>{{ movie.description|truncatewords:50 }}</video:description>
      <video:content_loc>{{ domain }}{{ movie.trailer_url }}</video:content_loc>
      <video:player_loc>{{ domain }}/player/{{ movie.movie_id }}</video:player_loc>
      <video:duration>{% if movie.runtime %}{{ movie.runtime }}{% else %}120{% endif %}</video:duration>
      <video:publication_date>{% if movie.release_date %}{{ movie.release_date|date:"Y-m-d" }}{% else %}2023-10-01{% endif %}</video:publication_date>
      <video:family_friendly>yes</video:family_friendly>
      <video:rating>{% if movie.rating %}{{ movie.rating }}{% else %}4.5{% endif %}</video:rating>
      <video:view_count>{% if movie.views %}{{ movie.views }}{% else %}1000{% endif %}</video:view_count>
      {% for genre in movie.moviegenres_set.all %}
      <video:tag>{{ genre.genre.name }}</video:tag>
      {% endfor %}
      <video:category>{% if movie.moviegenres_set.first %}{{ movie.moviegenres_set.first.genre.name }}{% else %}Phim{% endif %}</video:category>
      <video:uploader info="{{ domain }}/about">SMovie</video:uploader>
      <video:live>no</video:live>
    </video:video>
  </url>
  {% endfor %}
</urlset>
